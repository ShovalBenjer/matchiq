# Optional integrations. Each is wrapped by an in-package fallback, so the
# pipeline degrades gracefully when a dependency (or API key) is absent.
duckdb>=0.9            # analytical feature store (falls back to pandas)
pyarrow>=12           # parquet IO for the historical corpus
pyyaml>=6             # YAML config (falls back to JSON / dataclass defaults)
requests>=2.28        # REST/CSV data-source adapters
scikit-learn>=1.3     # TabPFN/ensemble fallback learners
anthropic>=0.39       # Claude-powered RAG news/injury agent
# Foundation models (large; optional). Adapters fall back without them:
# tabpfn>=2.0
# chronos-forecasting>=1.2
# torch>=2.0
